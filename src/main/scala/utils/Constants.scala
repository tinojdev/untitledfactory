package utils

import scalafx.scene.text.Font

object Constants {
  val gridSize = 25 // Can be increased but not decreased
  val itemSpeed = 20 // Time to travel one square (smaller = faster)
  val zoomSpeed = 5
  val itemMaxHeight = 5 // Amounts to how much items are scaled up when moving
  val screenHeight = 720
  val screenWidth = 1280
  val globalFont = Font.font("Dubai Medium")
  val smelterySpeed = 100
  val generatorSpeed = 30

}
